<%= form_with(model: tournament, local: true) do |form| %>
  <% if tournament.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tournament.errors.count, "error") %> prohibited this tournament from being saved:</h2>

      <ul>
      <% tournament.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :tournament_name %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :tournament_date %>
  </div>
  
  <div class="field">
    <%= form.label :time %>
    <%= form.time_select :time, id: :tournament_time %>
  </div>
  
  <div class="field">
    <%= form.label :location_id %>
    <%= form.hidden_field :location_id , value: @location.id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
